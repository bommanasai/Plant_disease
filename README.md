# Plant_disease
1) Dataset is downloaded from Kaggel
2) Data preprocessing (Normalization(scaling,sizing), Data Agumentation)
3) Build CNN model and trained dataset
4) Trained Dataset using pretrained models(VGG16,ResNet50,Inception)
5) PLotted training, validation accuarcy and loss
